mss>=9.0
opencv-python>=4.8
numpy>=1.24
pyautogui>=0.9
pynput>=1.7
Pillow>=10.0

# Platform window tracking. Without these the macro cannot locate the game
# window, and every ROI — stored as a fraction of that window — is uncomputable.
pyobjc-framework-Quartz>=9.0; sys_platform == "darwin"
python-xlib>=0.33; sys_platform == "linux"
